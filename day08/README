

FTP server
1、读取文件名
2、检查文件是否存在
3、打开文件
4、检查文件大小
5、发送文件大小和md5sum说给客户端
6、等客户端确认
7、开始边读边发


问题：
client 端收到的文件大小为空

#ftpserver作业
处理到 ftp_server 的core目录下的ftp_server.py文件。根据main函数来一步步走。

把作者的代码，复制过来。先跑一下。
ftp_server/bin/run.py是server端的入口，这样的话，前面声明了环境变量，就可以使用了。
不然单独运行的时候，会报错，找不到路径。



