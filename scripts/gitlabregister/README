文档：
http://python-gitlab.readthedocs.io/en/stable/api-usage.html

#用gitlab-python模块，要加个api-server=v3版本

xurl = "%s/api/v3/projects?private_token=%s" %(url,token)
x = requests.get(xurl)
#两种都可以，直接返回不是想要的内容
content = x.json()
content2 = x.text
print(content)


